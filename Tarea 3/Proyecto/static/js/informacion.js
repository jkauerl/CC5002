const cambiarPaginaDonacion = (id) => {
    window.location.href = "/informacion-donacion/" + id;
  }

const cambiarPaginaPedido = (id) => {
    window.location.href = "/informacion-pedido/" + id;
};
